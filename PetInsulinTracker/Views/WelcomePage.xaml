<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             x:Class="PetInsulinTracker.Views.WelcomePage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{DynamicResource PageBackground}">

    <VerticalStackLayout VerticalOptions="Center" Spacing="24" Padding="32">

        <mi:MauiIcon Icon="{mi:Fluent AnimalDog24}" IconSize="64" IconColor="{DynamicResource PageTextSecondary}" HorizontalOptions="Center" />

        <Label Text="Welcome to Pet Insulin Tracker"
               FontSize="24" FontFamily="OpenSansSemibold"
               HorizontalOptions="Center" HorizontalTextAlignment="Center" />

        <Label Text="Before we get started, let us know your name so we can track who logs each entry."
               Style="{StaticResource SubtleTextStyle}" FontSize="14"
               HorizontalOptions="Center" HorizontalTextAlignment="Center" />

        <Border Style="{StaticResource CardStyle}" Padding="16">
            <VerticalStackLayout Spacing="8">
                <Label Text="Your Name" Style="{StaticResource FormLabelStyle}" />
                <Entry x:Name="NameEntry" Placeholder="e.g., James"
                       FontSize="18" HorizontalTextAlignment="Center"
                       ReturnType="Done"
                       Completed="OnNameCompleted" />
            </VerticalStackLayout>
        </Border>

        <Button Text="Get Started"
                Style="{StaticResource PrimaryButtonStyle}"
                HorizontalOptions="Fill"
                Clicked="OnGetStartedClicked" />

    </VerticalStackLayout>

</ContentPage>
