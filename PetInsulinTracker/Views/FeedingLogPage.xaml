<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PetInsulinTracker.ViewModels"
             xmlns:models="clr-namespace:PetInsulinTracker.Models"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             x:Class="PetInsulinTracker.Views.FeedingLogPage"
             x:DataType="vm:FeedingLogViewModel"
             Title="Feeding Log"
             BackgroundColor="{DynamicResource PageBackground}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <!-- Log New Entry -->
            <Label Text="LOG FEEDING" Style="{StaticResource OverlineStyle}" />

            <Border Style="{StaticResource CardStyle}">
                <VerticalStackLayout Spacing="12">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Food Name" Style="{StaticResource FormLabelStyle}" />
                        <Entry Text="{Binding FoodName}" Placeholder="e.g., Hill's w/d" />
                    </VerticalStackLayout>

                    <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                        <VerticalStackLayout Grid.Column="0" Spacing="4">
                            <Label Text="Amount" Style="{StaticResource FormLabelStyle}" />
                            <Entry Text="{Binding Amount}" Keyboard="Numeric" Placeholder="Amount" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1" Spacing="4">
                            <Label Text="Unit" Style="{StaticResource FormLabelStyle}" />
                            <Picker ItemsSource="{Binding UnitOptions}"
                                    SelectedItem="{Binding Unit}" WidthRequest="90" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="2" Spacing="4">
                            <Label Text="Type" Style="{StaticResource FormLabelStyle}" />
                            <Picker ItemsSource="{Binding FoodTypeOptions}"
                                    SelectedItem="{Binding FoodType}" WidthRequest="80" />
                        </VerticalStackLayout>
                    </Grid>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                        <VerticalStackLayout Grid.Column="0" Spacing="4">
                            <Label Text="Date" Style="{StaticResource FormLabelStyle}" />
                            <DatePicker Date="{Binding LogDate}" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1" Spacing="4">
                            <Label Text="Time" Style="{StaticResource FormLabelStyle}" />
                            <TimePicker Time="{Binding LogTime}" />
                        </VerticalStackLayout>
                    </Grid>

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Notes" Style="{StaticResource FormLabelStyle}" />
                        <Entry Text="{Binding Notes}" Placeholder="Optional notes" />
                    </VerticalStackLayout>

                    <Button Text="Save" Command="{Binding SaveLogCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            HorizontalOptions="Fill" />
                    <HorizontalStackLayout Spacing="6" IsVisible="{Binding IsSyncing}" HorizontalOptions="Center">
                        <ActivityIndicator IsRunning="{Binding IsSyncing}" HeightRequest="14" WidthRequest="14"
                                           Color="{DynamicResource Primary}" />
                        <Label Text="Syncing…" Style="{StaticResource SubtleTextStyle}" FontSize="12" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- History -->
            <Label Text="HISTORY" Style="{StaticResource OverlineStyle}" Margin="0,8,0,0" />

            <CollectionView ItemsSource="{Binding Logs}" SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:FeedingLog">
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="Delete"
                                               BackgroundColor="{DynamicResource CurrentDanger}"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type vm:FeedingLogViewModel}}, Path=DeleteLogCommand}"
                                               CommandParameter="{Binding .}" />
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <Border Style="{StaticResource CardStyle}">
                                <Grid ColumnDefinitions="40,*" ColumnSpacing="12">
                                    <Border Grid.Column="0"
                                            BackgroundColor="{AppThemeBinding Light=#E8F5E9, Dark=#1B3D24}"
                                            StrokeThickness="0" StrokeShape="RoundRectangle 10"
                                            HeightRequest="40" WidthRequest="40">
                                        <mi:MauiIcon Icon="{mi:Fluent Food24}" IconSize="22" IconColor="{DynamicResource PageText}" HorizontalOptions="Center" VerticalOptions="Center" />
                                    </Border>
                                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="2">
                                        <Label FontFamily="OpenSansSemibold" FontSize="15">
                                            <Label.Text>
                                                <MultiBinding StringFormat="{}{0} — {1} {2}">
                                                    <Binding Path="FoodName" />
                                                    <Binding Path="Amount" />
                                                    <Binding Path="Unit" />
                                                </MultiBinding>
                                            </Label.Text>
                                        </Label>
                                        <HorizontalStackLayout Spacing="8">
                                            <Border BackgroundColor="{DynamicResource CurrentPrimary}"
                                                    StrokeThickness="0" StrokeShape="RoundRectangle 8"
                                                    Padding="8,2">
                                                <Label Text="{Binding FoodType}" Style="{StaticResource BadgeTextStyle}" />
                                            </Border>
                                            <Label Text="{Binding FedAt, StringFormat='{0:g}'}"
                                                   Style="{StaticResource SubtleTextStyle}" VerticalOptions="Center" />
                                            <Label Text="{Binding LoggedBy, StringFormat='· {0}'}"
                                                   Style="{StaticResource SubtleTextStyle}" VerticalOptions="Center"
                                                   IsVisible="{Binding LoggedBy, Converter={StaticResource IsNotNullOrEmptyConverter}}" />
                                        </HorizontalStackLayout>
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
